{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npm ci"
  },
  "deploy": {
    "startCommand": "cd backend && node src/index.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
